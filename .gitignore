# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
releases/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# local reference (do not commit)
build_ref.md

# TDLib per-user session files (local gateway)
.tdlib-data/

# generated when mirroring CI before npm pack (see README Milestone snapshot section)
fullREADME.md

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files (secrets; do not commit)
.env
.env*.local

# typescript
*.tsbuildinfo

app-example

# local scratch / temp
.tmp/

# Windows: reserved device name (accidental `> nul` in repo root)
/nul

# local debug / test artifacts (voice dialog, deploy, session mint)
bash.exe.stackdump
mint-session-out.*
*-out.txt
debug-voice-browser-out.txt
debug-voice-screens/

# Datadog: local process export (scripts/datadog-export-processes.ps1)
dd-processes-sample.txt

.vercel

# Terraform / GCP (local state and SA keys)
infra/gcp/terraform/.terraform/
infra/gcp/terraform/*.tfstate
infra/gcp/terraform/*.tfstate.*
infra/gcp/terraform/terraform.tfvars
**/wallet-kms-unwrap-sa-key.json
wallet-kms-unwrap-sa-key.json

.vercel
